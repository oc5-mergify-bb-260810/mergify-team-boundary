pull_request_rules:
  - name: BB required team boundary
    conditions:
      - title = BB required team gate canary
    actions:
      merge:
        method: merge

merge_protections_settings:
  reporting_method: check-runs
  post_comment: true
  auto_merge_conditions: true

merge_protections:
  - name: BB P21 base gate
    if:
      - base = main
    success_conditions:
      - base = main

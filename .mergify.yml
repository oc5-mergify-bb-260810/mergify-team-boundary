pull_request_rules:
  - name: BB required team boundary
    conditions:
      - title = BB required team gate canary
    actions:
      merge:
        method: merge
  - name: h1lab2 required team boundary
    conditions:
      - title = h1lab2 required team gate canary
    actions:
      merge:
        method: merge

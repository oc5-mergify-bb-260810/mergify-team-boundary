pull_request_rules:
  - name: BB required team boundary
    conditions:
      - title = BB required team gate canary
    actions:
      merge:
        method: merge

  - name: BB merge-bot current permission boundary
    conditions:
      - title = BB merge bot current permission gate
    actions:
      merge:
        method: merge
        merge_bot_account: onecursor6-oss

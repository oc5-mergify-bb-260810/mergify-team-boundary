merge_queue:
  reset_on_external_merge: always

pull_request_rules:
  - name: BB required team boundary
    conditions:
      - title = BB required team gate canary
    actions:
      merge:
        method: merge

queue_rules:
  - name: default
    batch_size: 2
    batch_max_wait_time: 2 minutes
    queue_conditions: true
    merge_conditions:
      - check-success = BB_EXTERNAL_MERGE_RESET_RELEASE_20260816
    allow_queue_branch_edit: false
    merge_method: merge-batch
    checks_timeout: null

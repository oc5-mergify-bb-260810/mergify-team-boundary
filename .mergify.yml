pull_request_rules:
  - name: BB required team boundary
    conditions:
      - title = BB required team gate canary
    actions:
      merge:
        method: merge

queue_rules:
  - name: default
    batch_size: 2
    batch_max_wait_time: 1 minute
    queue_conditions: true
    merge_conditions:
      - check-success = BB_DRAFT_BOT_RELEASE_20260816
    merge_method: merge-batch
    checks_timeout: null
    draft_bot_account: onecursor6-oss
